import { useNavigate } from "react-router-dom"

export default function UnauthorizedServerError({flag, path}){

    const navigate = useNavigate();

        return(
            {
                if (flag){
                        
                }
            }
    )

}